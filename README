* start server.js via Node.js
* open index.html on your browser
* in "server.js" the default database is set to "information_schema"
* type "show tables" e.g. in the lower input and see the result in the upper output area
